name: Basic workflow
on:
   push:
      branches:
        - master
        - devops
        - whatever
jobs:
  simplejob:
    runs-on: self-hosted
    steps:
    - name: greeting step
      run: echo "welcome world"
      